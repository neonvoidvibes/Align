import SwiftUI

struct UIStyles {
    /// A background color suitable for cards, adapting to light/dark mode.
    static let cardBackground = Color(UIColor.systemGray6)

    /// Standard corner radius for cards.
    static let cardCornerRadius: CGFloat = 10

    /// Standard padding for card content.
    static let cardPadding: EdgeInsets = EdgeInsets(top: 12, leading: 16, bottom: 12, trailing: 16)

    // Add more shared styles here as needed
}